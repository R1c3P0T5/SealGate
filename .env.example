# Root environment variables for docker-compose.yml
# Copy to .env and fill in values

# Cloudflare Tunnel token (obtain from Cloudflare Zero Trust dashboard)
# Required only for Compose profiles that include the cloudflared service.
# Local Docker development normally uses `make dev`.
CLOUDFLARE_TUNNEL_TOKEN=<token-from-cloudflare-dashboard>
